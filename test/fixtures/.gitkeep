# Placeholder — keeps this directory in git for the
# `Horde\Bundle\Test\Fixtures\` PSR-4 autoload mapping declared in
# ../../composer.json. PHP fixtures for future bundle-level tests
# land here. The previous shell config fixtures (apache-horde.conf,
# nginx-horde.conf) are now generated by `hordectl webserver-config`
# instead of hand-maintained.
